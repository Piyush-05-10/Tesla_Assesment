kmeans:
  n_clusters: 8
  n_init: "auto"
  max_iter: 300
  random_state: 42

hdbscan:
  min_cluster_size: 15
  min_samples: 5
  metric: "euclidean"
  cluster_selection_method: "eom"
  prediction_data: true

evaluation:
  silhouette: true
  calinski_harabasz: true
  sample_grid_size: 20
